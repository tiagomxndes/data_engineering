"""
Exercise 4 — Functions with default/keyword arguments (since this is what tripped you up earlier — let's cement it)

Write a function format_price(amount, currency="USD", decimals=2) that:
Returns a string like "19.99 USD" — the amount rounded to decimals places, followed by the currency code
currency and decimals should have the defaults shown, so format_price(19.999) → "20.00 USD"
Then call it three ways: once with just amount, once overriding currency only by keyword,
and once overriding both by keyword in reversed order from the definition.
"""


def format_price(amount, currency="USD", decimals=2):
    pass
